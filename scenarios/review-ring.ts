import type { ScenarioDefinition } from "./types.js";

export const reviewRingScenario: ScenarioDefinition = {
  id: "review-ring",
  name: "Fake Review Ring",
  description: "Buyer and seller coordination centered on review manipulation.",
  datasetPath: "/datasets/review-ring",
};
