# Datasets

Reusable raw event datasets live here.

## Scenarios

- `review-ring/`
- `refund-abuse/`
- `seller-collusion/`

Each scenario folder contains deterministic JSON files that Person 1 can seed into the database for shared feature development.
