import type { ScenarioDefinition } from "./types.js";

export const sellerCollusionScenario: ScenarioDefinition = {
  id: "seller-collusion",
  name: "Seller Collusion",
  description: "Coordinated seller behavior with overlapping devices and reviews.",
  datasetPath: "/datasets/seller-collusion",
};
